## 프로토타입에 대해

- 프로토타입은 핵심적인 기능이 우선적으로 구현이 되어야 함
- 해당 시스템은 SLM에 따라 도메인 지식과 경험에 기반한 답변이 가장 중요한 기능임
- 다시 말해 SLM이 답변을 생성하기 위해 도큐먼트의 범위를 반영할 수 있는 기능이 가장 주요함
- 여기에 더해 사용자가 SLM에 데이터를 전달하는 인터페이스와 사용자가 효과적으로 SLM의 답변을 확인할 수 있는 인터페이스의 구현이 핵심이 되어야 함
- 그렇다면 프로토타입에서는 이 파이프라인의 설계와 구현이 중심이 되어야 함
- 파이프라인을 만들기 위해 실무적으로 다음의 순서로 진행될 것으로 예상

1) HTTP 클라이언트로 Any API 호출
2) 호출된 Any API는 데이터베이스에 호출정보를 저장
3) 데몬이 데이터베이스를 주기적으로 리딩해서 Clas-SLM에게 작업을 할당
4) Clas-SLM은 작업상태를 업데이트하면서 작업을 처리
5) Clas-SLM의 처리가 끝나면 Plan-SLM이 작업계획
6) Plan-SLM의 처리가 끝나면 Proc-SLM이 계획된 작업을 실행하여 결과를 산출

근데 꼭 SLM들을 역할별로 나눠야 하는가?
역할별로 나눌 때의 이점은 무엇인가?

하나의 SLM이 모든 단계를 처리한다면 병목이 발생할 수 있지 않을까?
그러나 어쨌든 Plan이나 Proc에서는 병목이 발생할 수 없음
여러 형태의 SLM을 띄웠을 때는 운영의 난이도가 높아짐
소스코드는 비교적 간결해지기 때문에 유지보수 관점에서는 효과적임
확장성의 관점에서는 필요한 SLM의 볼륨에 집중하여 늘릴 수 있으므로 더 효율적일 것으로 판단됨

그렇다면 각 SLM마다 적절한 개수는?
각 SLM의 평균 처리 시간을 비교해 각 SLM의 개수를 할당해야 함

SLM 프로세스 풀을 만들어야 함
잠깐, 그런데 프로세스를 여럿 띄워도 SLM이 하나면 소용이 없지 않은가? SLM Pool도 필요한가?


VRAM에 맞게 SLM을 몇 개를 올려둘지도 결정을 해야함
만약 이렇게 여러 개의 SLM을 VRAM에 올려둔다면, 굳이 SLM 형태별 Pool은 필요하지 않음. 그런데 SLM을 VRAM에 여러 개를 올릴 수 없다고 한다면, SLM을 형태별로 분리하는 게 과연 의미있는 작업인가?


큰 의미는 없을 것으로 보임.

Clas와 Plan, Proc에 대한 작업들이 이어질 것이고,
이 작업들에 대한 로그를 중간 중간 찍어줘야 할 것임
이 로그는 무엇으로 찍는가? DB? KAFKA? FILE?
어떤 걸로도 무방하다고 보지만, DB는 아니고, FILE도 아님.
DB와 FILE은 동시성 이슈로 인해 병목이 발생함
KAFKA는 너무 무겁지 않을까? 무겁다는 이유만으로 용도의 유용함을 무시할 수는 없으며, 그리고 KAFKA가 무겁다는 건 전적으로 오해. 이 SLM S/W는 클러스터 상에 구축하지 않기 때문에 무겁지 않음

그럼 기술적으로 현재 실험을 해봐야 할 곳은 어디인가?


1. 먼저 Relay Server 더미 만들기
2. HTTP 클라이언트로 API 호출하기
- 질문, 첨부파일을 API를 통해 데이터베이스 및 파일시스템에 등록
3. vLLM으로 Gemma 올리기
4. API 호출을 받아 vLLM으로 Gemma 호출하기

- Hugging Face API Key: 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

export HF_TOKEN="hf_your_actual_token_here"

# Gemma 2 2B Instruct 모델 서빙 예시 (VRAM 절약용)
vllm serve google/gemma-2-2b-it \
    --port 8000 \
    --trust-remote-code \
    --gpu-memory-utilization 0.9























- Clas-SLM은 API 스펙을 학습해서 사용자가 보낸 질문을 분석하여 